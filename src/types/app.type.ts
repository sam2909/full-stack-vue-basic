export type Cart = {
    id: number
    img: string,
    name: string,
    price: number,
    quantity: number
}

export type Product = {
    id: number
    img: string,
    name: string,
    price: number,
    stars: number,
    un: string
}